软件结构说明
一、系统软件结构
1.basic_conponent         主要用来定义机器人A、B臂，主机箱、操作臂等组件，并定义各个组件的动作、传感器信息；
2.basic_data_type         主要用来存储公共的数据类型，用于后续相关函数使用；
3.basic_instruct_module   主要用来定义网络通信、基础指令的收发；
4.basic_motion            主要用来定义基础动作库、包括步序动作、八种基础状态，以及各种状态之间的相互转换；
5.control_module          主要用来定义控制算法，包括行走控制以及越障控制逻辑等相关的可执行代码文件；
6.cvision                 主要用来定义视觉相关算法，包括障碍物测距、定位、类别判断等工作；
7.logs                    主要用来存储系统日志：包括控制日志等；
8.motordriven             驱动层，主要用来驱动电机，通过spi读写FPGA；
9.navigation              导航模块，主要用来地图下载、更新位置信息等；
10.slam                   slam，暂不支持；
11.urdf                   机器人仿真，暂不支持；
12.state_detection_module 机器人状态检测，用于检测机器人的各种状态、如电量信息、动作约束等。
